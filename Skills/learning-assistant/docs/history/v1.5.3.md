# v1.5.3 — 2026-09-07 真机反馈修复（ask_user/错别字/见解/合并/公式）

- 变更：ask_user 每次调用必带 text 项（single/multi 末选项兜底“我自己说”）；错别字修正（精记→记录、判型→判定类型、搬进→搬入）+ 歧义收紧（find 精确路径、复用阈值、异构题定义）；出题降级可选（默认难度1-2）。
- 表结构：+insights 表（用户原话理解，照录禁改写）+ 索引 1 个；旧库重跑即得，零 ALTER。
- 行为：精查子查询带回最新见解并引用；主动合并事务（关联转指后删旧行，times_asked/计数相加）；公式禁用 `$` `\` 命令改 Unicode。
