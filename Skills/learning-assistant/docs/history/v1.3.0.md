# v1.3.0 / v2.4.0 — 2026-09-06 移除 Node 依赖，sqlite3 CLI 直连

- 变更：删除两技能 db.js/selfcheck.js（英语含 migrate.js）；package.json 去 dependencies；SYSTEM_PROMPT 重写为 CLI 三式；SKILL 加 SQL 操作参考。
- 表结构：无变更（沿用旧表，CLI 直读写）。
- 行为：零 npm 依赖，适配 RikkaHub rootfs（Node 18 装不上 better-sqlite3）。
