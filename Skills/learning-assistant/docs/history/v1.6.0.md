# v1.6.0 — 2026-09-16 触发器瘦身+强制确认+公式事故修复

- 变更：+6 触发器（progress 重算×2、qa 日志×2 含追问累加、mistake 日志×2 含复犯累加），AI 只 INSERT 原子事实；heredoc 标准头（.bail on/定界符禁转义/内容禁英文'/quoted 定界符/同进程取 id）。
- 表结构：仅加触发器，重跑即得，零 ALTER；回滚用 DROP TRIGGER。
- 行为：终答后强制 ask_user 确认掌握（懂了记 correct，半懂/没懂记 wrong+mistakes）；写库必回执，重发不计数；调用压到首问 3 追问 2；公式行内 $ 禁空格、复杂式独立 $$、禁 boxed、表格内禁 $；exercise/review 日志仍手写（触发器不管），Review/Exercise 模块同步。
