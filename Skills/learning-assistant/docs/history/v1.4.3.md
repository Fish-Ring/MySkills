# v1.4.3 — 2026-09-06 提示词清晰化+主动提问

- 变更：`SYSTEM_PROMPT` 重构为编号+祈使句，首节“角色与技能绑定（最高优先级）”强调不写库视为未完成；新增“向用户提问”3时机（档案缺失/代码不认识/答疑后确认），文案AI或用户设置决定（单选/开放式）。
- 表结构：无变更。
- 行为：`questions` 问即疑 `wrong_count+1`；`SKILL` 约束同步为5条。（重命名 `english-vocabulary-coach`→`english-learning-assistant` 实际在同期完成，不属本版。）
