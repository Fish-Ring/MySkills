# v2.4.0 — 2026-09-06 移除 Node 依赖，sqlite3 CLI 直连

- 变更：删除 db.js/migrate.js/selfcheck.js（后重建为 sh 版）；package.json 去 dependencies；SYSTEM_PROMPT 重写为 CLI 三式。
- 表结构：无变更（沿用 words/review_queue/history_logs，CLI 直读写）。
- 行为：零 npm 依赖，适配 RikkaHub rootfs。
