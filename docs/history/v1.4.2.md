# v1.4.2 — 2026-09-06 分页+统计一体化

- 变更：分页统一 `COUNT(*)` 先行、`LIMIT 20 OFFSET n` 按需；新增统计总览 `qs/tps/ms/subs`、薄弱综合 `ORDER BY wrong_count,d`；`tag` 约束收敛为专业名词 `1主+最多5细分`。
- 表结构：`topics/questions.tags` 新增，索引 `idx_topics_subject` 等 9 个；`schema.sql` 幂等。
- 行为：精查 `IN 2-3要点 + tag LIKE 兜底`；记忆仍为计数+Top6（后被 v1.4.4 收敛为白名单）。
