# v1.4.4 — 2026-09-06 技巧表+白名单+去队列+复盘

- 变更：记忆刷新收敛为白名单（基础信息22408/7科/阶段 + DB概况计数qs/tps/ms/tcs + 薄弱Top6名称+掌握度），其余永不进记忆；`review_queue` 通用技能废弃（兼容保留不写入）。
- 表结构：新增 `techniques(primary_subject_id NULL=通用 UNIQUE)` + `technique_topics/technique_questions` 双M:N `PRIMARY KEY` +6索引；`schema.sql` 幂等重跑即可。
- 行为：技巧入库 AND门控（跨3异构题复用 +2-5步动词化 +IF-THEN含主标签，缺一不入），查重 `lower(trim)/别名/tag交集 LIMIT 5` 合并，跨科由AI自主多关联一行；新增每日复盘（`history_logs+questions/progress` 聚合，仅“复习/复盘/总结”时触发）。
