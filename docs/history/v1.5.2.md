# v1.5.2 — 2026-09-06 四路全量审计修复

- 变更：索引二次瘦身（M:N 反向纠正、删 history/mistakes 单列与旧 mastery/status 单列、补 3 表达式索引，共 18 个）；错题复犯累加（mistake_count upsert）；progress 建行含连击维护；精查 LIMIT 5；档案 INSERT/UPDATE 模板补齐。
- 表结构：仅索引变化，重跑即生效；mistakes/history_logs 的 NULL 唯一坑维持现状（COALESCE 查重覆盖）；声明要求 SQLite ≥3.24（表达式索引 ≥3.9）。
- 行为：薄弱口径统一 status/score；提问单选必带兜底；输出示例分隔符改 `·`；last_asked_at 按 UTC 比对；英语技能 Review 伪代码可执行化（:is_correct 替换）。
